# Network and Asynchronous Federation

The network layer models availability, latency, jitter and update age abstractly. Asynchronous aggregation applies an exponential staleness weight.

This is a research abstraction, not a packet-level or protocol-level network simulator. The intended questions are how delayed participation changes trust, whether slow benign sites are misclassified, and whether staleness-aware weighting improves stability.
